load("isPalindrome.js")

var word = "hello";
if (isPalindrome(word)) {
    print(word + " is a palindrome.")
} else {
    print(word + " is not a palindrome.")
}
word = "racecar";
if (isPalindrome(word)) {
    print(word + " is a palindrome.")
} else {
    print(word + " is not a palindrome.")
}
